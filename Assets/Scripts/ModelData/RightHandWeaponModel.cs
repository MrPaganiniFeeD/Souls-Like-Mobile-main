using System.Collections;
using System.Collections.Generic;
using Hero.States;
using UnityEngine;
using Zenject;

public class RightHandWeaponModel : MonoBehaviour
{ 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
